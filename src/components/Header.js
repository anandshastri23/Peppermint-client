import React from "react";

import "../App.css";

const Header = () => (
    <div className = "navbar">
        <marquee>
            <blink>This is where the Header goes </blink>
         </marquee>
     </div>
);

export default Header;